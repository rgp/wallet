<%= form_for @w_transaction, :html => { :class => "form-horizontal w_transaction" } do |f| %>

    <% if @w_transaction.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@w_transaction.errors.count, "error") %> prohibited this w_transaction from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @w_transaction.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :pocket_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :pocket_id, :class => 'form-control' %>
    </div>
    <%= error_span(@w_transaction[:pocket_id]) %>
  </div>
  <div class="control-group">
    <%= f.label :amount, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :amount, :class => 'form-control' %>
    </div>
    <%= error_span(@w_transaction[:amount]) %>
  </div>
  <div class="control-group">
    <%= f.label :tipo, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tipo, :class => 'form-control' %>
    </div>
    <%= error_span(@w_transaction[:tipo]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            w_transactions_path, :class => 'btn btn-default' %>

<% end %>
